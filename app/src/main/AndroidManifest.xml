<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ksk.daily_agenda">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Me" />
        <activity android:name=".Groups" />
        <activity android:name=".CreateGroup" />
        <activity android:name=".GroupActivity" />
        <activity android:name=".FridgeMe" />
        <activity android:name=".EditName" />
        <activity android:name=".Votings" />
        <activity android:name=".CreateVoting" />
        <activity android:name=".Vote" />
        <activity android:name=".Finances" />
        <activity android:name=".MainActivity"/>
        <activity android:name=".FridgeCreateItem"></activity>
    </application>

</manifest>